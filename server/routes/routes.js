const { Router } = require('express');

const serverRouter = Router();

// serverRouter.get();

// serverRouter.post();

// serverRouter.put();

// serverRouter.delete();


module.exports = {
  serverRouter,
};
