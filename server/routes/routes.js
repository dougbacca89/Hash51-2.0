const { Router } = require('express');

// const { Evidence } = require('../database/Evidence');

const serverRouter = Router();

// serverRouter.get();

// serverRouter.post();

// serverRouter.put();

// serverRouter.delete();


module.exports = {
  serverRouter,
};
